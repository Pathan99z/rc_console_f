/** localStorage keys used by auth + API client */
export const STORAGE_KEYS = {
  accessToken: 'accessToken',
  /** Legacy flag from Login/Signup — replace when auth API is wired */
  isAuth: 'isAuth',
  userName: 'userName',
}
